# BaoBaoClaude Configuration
# Copy this file to ~/.baobaobot/.env and fill in the values.

# Required: Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Required: Allowed Telegram User IDs (comma-separated)
ALLOWED_USERS=123456789,987654321

# Optional: Tmux session name (default: baobaobot)
TMUX_SESSION_NAME=baobaobot

# Optional: Claude command to run in new windows (default: claude)
CLAUDE_COMMAND=claude

# Optional: Monitor poll interval in seconds (default: 2.0)
MONITOR_POLL_INTERVAL=2.0

# --- BaoBao Workspace Settings ---

# Optional: Workspace directory (default: ~/.baobaobot/workspace)
# WORKSPACE_DIR=~/.baobaobot/workspace

# Optional: Daily memory retention in days (default: 30)
# MEMORY_KEEP_DAYS=30

# Optional: Recent memory days included in CLAUDE.md (default: 7)
# RECENT_MEMORY_DAYS=7

# Optional: Auto-assemble CLAUDE.md on session start (default: true)
# AUTO_ASSEMBLE=true
